
function sum(numOne, numTwo) {
    return numOne + numTwo; //Write this LAST
}

function difference(numOne, numTwo) {
    return numOne - numTwo;  //Write this LAST
}

function product(numOne, numTwo) {
    return numOne * numTwo; //This is LAST
}

function quotient(numOne, numTwo) {
    return numOne / numTwo; // This is LAST
}

export { sum, difference, product, quotient };